// Multiple Parameters

func timeToDestination(distance: Int, speed: Int) -> Int {
    let time = distance / speed
    return time // return the values from enclosing function
}

print(timeToDestination(distance: 6836, speed: 560))


Output:
12