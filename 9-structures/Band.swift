// Memberwise Initialization

/* using this  assign values upon instance creation. 
So in certain cases, we don’t actually need to use init()! */

// Add your code below 🎹 🎸 🥁 

struct Band {
    var genre: String
    var members: Int
    var isActive: Bool
}

// creating instance of Band
var maroon5 = Band(genre: "pop", members: 5, isActive: true)